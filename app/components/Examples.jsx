var React = require('react');
var {Link} = require('react-router');

var Examples = (props) => {
  return (
    <div>
      <h1 className="text-center">Examples</h1>
      <p>Here are no examples locations to try out :)</p>

    </div>
  )
};

module.exports = Examples;
